# Session Log — May 10, 2026

## What We Did
- Learned what a **terminal** and **shell** are, and how to type commands instead of clicking
- Learned what **GitHub** is — version control, snapshots (commits), branches, and pull requests
- Created the `greek_myth_ios_app` repo on GitHub and cloned it locally
- Learned the difference between `git` (tracks code history) and `gh` (talks to GitHub features)
- Set up `gh` CLI and authenticated with GitHub via browser
- Created `CLAUDE.md` (project guidance for Claude Code) and `brainstorm.md` (summer plan)
- Made our first real commit and pushed to GitHub
- Learned what **Claude Code** is and how to use it to scaffold and plan a project

## Key Commands Learned
```bash
git clone <url>        # download a repo
git status             # see what changed
git add <file>         # stage a file
git commit -m "..."    # save a snapshot
git push               # upload to GitHub
gh auth login          # authenticate with GitHub
```

## Next Session Ideas
- Pick the app idea from the brainstorm (quiz, card collector, adventure, or mini-game)
- Sketch a few screens on paper
- Isaac creates his first branch
