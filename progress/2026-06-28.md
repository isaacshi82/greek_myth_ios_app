# Session Log — June 28, 2026

## Focus: Toward a sellable product — content + UI cleanup

### Hid story mode
- Added a `showStoryMode` feature flag in `MainMenuView` (set to `false`). Hero cards are parked behind it; trivia is now the sole focus on the menu. Flip the flag to bring them back.

### Grew the trivia bank from 12 → 78 questions
Built it out in four batches of ~12–15, balanced across categories and difficulty:

| Category | Questions |
|---|---|
| Olympians | 15 |
| Heroes | 15 |
| Monsters | 15 |
| The Underworld | 15 |
| Titans & Creation | 15 |
| Myths & Stories | 3 |
| **Total** | **78** (31 easy / 32 medium / 15 hard) |

- Every question carries a `category`, `difficulty`, and `source` field.
- **Web-verified the contested/gotcha facts** rather than trusting recall: Hydra regrows two heads; Pandora's *jar* (Hope remains); Charon's river (Acheron in Greek sources, Styx in Roman — so we asked about the coin instead); Persephone's seed count varies (asked the fruit, not the number); Atlas holds the *sky* not the earth; Prometheus's liver eaten by an eagle; Achilles' heel is a *post-Homeric* (Statius) legend.
- Validated after every batch: unique IDs, exactly 4 answers each, valid correct index, no duplicate question text, no repeated options, all fields present.
- Confirmed the app launches and decodes all 78 (a bad JSON would crash on load).

## Product direction (discussed)
- Honest market read: myth-trivia is crowded; our moat is **art + accurate, source-cited learning + (later) story mode**.
- Decided art stays **AI-made in-house** (the hero portraits already look great); no designer hire. App icon = the one asset to treat carefully (1024×1024, no text, fill the square, don't pre-round).
- Likely monetization: **freemium** (free base categories; one-time unlock for all categories + Daily Challenge + eventual story mode). No ads.

## Things Isaac Should Know
- All questions live in `Data/trivia_questions.json` — add/edit as data, no Swift needed.
- A feature flag (`showStoryMode`) is a clean on/off switch for a whole feature.
- Citing a `source` per question is what keeps the content trustworthy (and catches movie-vs-myth errors).

## Next Session Ideas
- Review the 66 new questions with Isaac (fun/fairness/age check); swap any that feel off.
- Generate the app icon (golden Corinthian helmet) and wire it into `Assets.xcassets`.
- Build the **category picker** + **difficulty filter** (schema already supports it).
- Save best score / add a Daily Challenge for retention.
